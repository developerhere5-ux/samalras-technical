# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.zip

node_modules
*.local
dist.zip
/data

# Next.js build output — never commit these.
# `.next/` is the build cache, `out/` is the exported static site.
/.next/
/out/
/next-env.d.ts
*.tsbuildinfo

# Every dot-folder (tooling, editors, AI assistants, build caches).
# Add a `!.name/` exception below if one ever needs to be committed.
.*/
!.gitignore

# Named explicitly so the intent survives if the catch-all above is relaxed
.claude/
.remember/
.understand-anything/
.ua/
.cache/
.history/
.playwright-mcp
.vercel/
CLAUDE.md

# Build machinery (kept out of the repo)
scripts/
lib/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
./*

assets-original/
